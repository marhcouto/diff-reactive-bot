properties: {}
layers:
  - name: ["static", "no_laser"]
    map: "map.yaml"
    color: [1.0, 1.0, 1.0, 1]
models:
  - name: serp
    model: "serp.model.yaml"
    pose:
      - $eval env("ROBOT_POS_X", 5.0)
      - $eval env("ROBOT_POS_Y", 4.0)
      - 0.0

    # Test values
    # pose: [6.2, 0.0, 0]
    # pose: [5, -4.0, 0]
    # pose: [3, -2.0, 0]
